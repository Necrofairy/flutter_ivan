import 'package:flutter/material.dart';

class SelectorChatRoomPage extends StatelessWidget {
  const SelectorChatRoomPage({Key? key}) : super(key: key);
  static const routeName = '/selector';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
