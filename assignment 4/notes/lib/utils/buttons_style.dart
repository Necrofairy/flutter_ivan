import 'package:flutter/material.dart';

class ButtonsStyle {
  static final googleButton = ButtonStyle(backgroundColor: MaterialStateProperty.all(Colors.black));
  static final confirmButton = ButtonStyle(backgroundColor: MaterialStateProperty.all(Colors.black));
}