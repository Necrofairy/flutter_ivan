import 'package:flutter/material.dart';

abstract class ColorsRGB {
  static const background = Color.fromRGBO(255, 255, 255, 1.0);
  static const appBar = Color.fromRGBO(203, 144, 219, 1.0);
  static const buttonsActive = Color.fromRGBO(203, 144, 219, 1.0);
  static const activeCardShadow = Colors.purple;
  static const passiveCardShadow = Colors.black;
}
